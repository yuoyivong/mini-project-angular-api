services:
  postgres:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER : postgres
      POSTGRES_PASSWORD : 1231
      POSTGRES_DB : product_order_db
    ports:
      - "5433:5432"
    volumes:
      - db:/var/lib/postgresql/data
volumes:
  db:
