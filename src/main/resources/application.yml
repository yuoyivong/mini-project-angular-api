spring:
  application:
    name: spring-mini-project
  datasource:
    url: jdbc:postgresql://localhost:5433/mini_project_db
    username: postgres
    password: 1231
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
